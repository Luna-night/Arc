[package]
name = "arcaea-os"
version = "0.1.0"
edition = "2021"

# 【核心策略】不依賴任何第三方 OS 庫 (sbi-rt, riscv-rt 等)
# 完全使用 Rust 核心庫 (core) 和手寫匯編，確保絕對的編譯穩定性
[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"