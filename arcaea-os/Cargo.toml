[package]
name = "arcaea-os"
version = "0.1.0"
edition = "2021"

# 【核心確認】RISC-V 架構使用 OpenSBI，絕對不需要 bootloader crate！
# 移除所有依賴，讓 serde_core 的幽靈徹底消散
[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "s"